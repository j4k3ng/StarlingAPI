import React from "react"
import ReactDOM from "react-dom"
import App from "./App"

/**
 * Render the App.js component into the "root" to index.html.
 */
ReactDOM.render(<App />, document.getElementById("root"))